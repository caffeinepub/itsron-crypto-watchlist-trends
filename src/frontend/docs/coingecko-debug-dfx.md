# CoinGecko Debug Testing Guide

This document provides instructions for deploying and testing the raw CoinGecko API responses using the `debugFetchCoinGecko` function.

## Prerequisites

- Ensure you have `dfx` installed and configured
- You must be authenticated as an admin user to call the debug function

## Deployment

Deploy the backend canister:

